import React from 'react'
import data from '../sample.json'//imported json data of news

export default function News() {
    const articlesArray = data.articles; //{an array of object which contains data which will be populated}
    
  return (
    <div>
      
    </div>
  )
}
