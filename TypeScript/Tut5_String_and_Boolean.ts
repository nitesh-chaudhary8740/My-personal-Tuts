/**
 * Points to cover
 * Apply string datatype
 * Ways to define string
 * Convert in string datatype
 * Apply boolean datatype
 * Possible boolean values
 * Inference and declaration issues
 * Run with html file
 */
var str:string="hello";
var num:number=25;
//conversion
// var data:string = num;//err:different datatype
var data1:string = num.toString()
console.log(data)
var boolData:boolean = true;
var data2:string = boolData.toString();